{
  "name": "@ai-team/scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "CLI scripts for A(i)-Team board management",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
